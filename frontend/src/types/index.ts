export type Atrativo = {
  id: string;
  images: string[];
  category: string;
  title: string;
  excerpt: string;
  text: string;
};

export type Evento = {
  id: string;
  img: string;
  title: string;
  date: string;
  desc: string;
};
